export interface Product {
    name?: string,
    price?: string,
    availability?: number,
    image?: string,
    id: number,
    weight?: string,
    color?: string,
    producedYear?: string,
    waranty?: string,
}